<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>site.sorghum.agent</groupId>
        <artifactId>loopra</artifactId>
        <version>26.8.91</version>
    </parent>

    <artifactId>loopra-acp</artifactId>
    <description>Loopra ACP 支持：将 Agent 注册为 ACP（Agent Client Protocol）Agent，支持 stdio / WebSocket 传输</description>

    <dependencies>
        <!-- Loopra 内核 -->
        <dependency>
            <groupId>site.sorghum.agent</groupId>
            <artifactId>loopra-model</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- Loopra 工具装备（LoopraAgent/会话/工作区/配置等编排设施） -->
        <dependency>
            <groupId>site.sorghum.agent</groupId>
            <artifactId>loopra-harness</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- ACP Agent SDK -->
        <dependency>
            <groupId>com.agentclientprotocol</groupId>
            <artifactId>acp-core</artifactId>
        </dependency>
        <dependency>
            <groupId>com.agentclientprotocol</groupId>
            <artifactId>acp-agent-support</artifactId>
        </dependency>
        <dependency>
            <groupId>com.agentclientprotocol</groupId>
            <artifactId>acp-annotations</artifactId>
        </dependency>

        <dependency>
            <groupId>org.noear</groupId>
            <artifactId>solon-test-junit5</artifactId>
            <version>${solon.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

</project>
